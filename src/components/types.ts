
export interface MarketInsight {
  headline: string;
  content: string;
  source?: string;
  url?: string;
  title?: string; // For backward compatibility
}

export interface NewsItem {
  headline: string;
  content: string;
  source?: string;
  url?: string;
  title?: string; // For backward compatibility
}
