project_id = "jhtnruktmtjqrfoiyrep"

# Existing configurations remain the same

# Add this new CORS configuration
[[headers]]
for = "/functions/*"
[headers.values]
Access-Control-Allow-Origin = "*"
Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"
Access-Control-Allow-Headers = "authorization, x-client-info, apikey, content-type, origin, x-requested-with, x-app-version"
Access-Control-Max-Age = "86400"
Vary = "Origin"

# If you have specific allowed origins, replace "*" with:
# Access-Control-Allow-Origin = "https://yourdomain.com, https://preview.yourdomain.com"
